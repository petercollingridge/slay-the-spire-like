# Phaser test

## To run

I'm using [http-server](https://www.npmjs.com/package/http-server).

To run the server, run `http-server .`

Or with python `python3 -m http.server`

Then view at http://127.0.0.1:8080.
